# Merchant Room Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the merchant room's placeholder panel ("Торговец скоро появится.") with the fully specified GDD 5.2 mechanic: 5 random items priced by `100 × itemLevel × tierMultiplier`, a 30%-chance single discount per visit (weighted discount-size table), purchase spending run currency, no selling, no ration cost.

**Architecture:** `RewardManager` gains a `GenerateMerchantOffers(int characterLevel)` method that reuses its existing `RollItemRarity`/`RollItemLevel` machinery (same rarity table as the chest, per GDD 5.2 — "по той же таблице, что и в сундуке"). `RunFlowController.MerchantRoomFlow` is rewritten to render 5 offer cards, handle purchase (spend currency, then route into the existing `ItemCompareFlow` slot-selection UI — the same "no inventory" flow chests already use), and let the player leave without buying.

**Tech Stack:** Unity 6000.5.8f1, C#, UI Toolkit.

**Spec:** ГДД Данжнгерлс (рабочая версия), section 5.2. User sync prompt item 5.

## Global Constraints

- Rarity table for merchant offers is the SAME as the chest table from Plan 1 Task 6 (62/35/3) — this plan must be applied AFTER that one, or the merchant will roll the old 60/30/10 table if applied first. Note the dependency at execution time.
- Item level range: `[characterLevel, characterLevel + 2]` inclusive — same formula the chest already uses (`RewardManager.RollItemLevel`).
- Price: `100 × itemLevel × multiplier`, multiplier = 1.0 (Common) / 1.2 (Rare) / 1.5 (Epic).
- Discount: rolled ONCE per merchant visit (not per item) — 30% chance that exactly one of the 5 offered items (chosen at random among the 5) gets a discount. Discount size table (independent of the 30% gate, applied only if the gate succeeds): 10% (50% weight), 25% (30%), 50% (14%), 75% (5%), 90% (1%) — sums to 100%.
- No selling — the player's own equipment can never be sold to the merchant (unchanged, was already true since there's no sell UI anywhere).
- Visiting the merchant room does NOT consume a ration (unchanged — `MerchantRoomFlow` never touches `CampManager`).

---

### Task 1: `RewardManager.GenerateMerchantOffers`

**Files:**
- Modify: `Assets/Scripts/Managers/RewardManager.cs`
- Test: `Assets/Editor/PlayModeSmokeTest.cs`

**Interfaces:**
- Consumes: `RewardManager.RollItemRarity(bool isBoss)` (existing, Plan 1 Task 6 updates its thresholds — this task doesn't touch it), `RewardManager.RollItemLevel(ItemData, int)` (existing private method — this task changes its accessibility, not its logic).
- Produces: `public class MerchantOffer { public ItemData Item; public int OriginalPrice; public int Price; public bool HasDiscount; }`, `RewardManager.GenerateMerchantOffers(int characterLevel)` → `List<MerchantOffer>`.

- [ ] **Step 1: Change `RollItemLevel` from `private` to accessible within the class (no signature change — it's already called from `CalculateRewards` in the same class, this step is a no-op unless the review below finds it needs to move; keep as-is if it already compiles when called from a new method in the same class)**

`RollItemLevel` is already an instance method on `RewardManager` (see `Assets/Scripts/Managers/RewardManager.cs:72`), so a new method also on `RewardManager` can call it directly without any accessibility change. No edit needed here — confirmed by reading the existing file before writing this plan.

- [ ] **Step 2: Add the `MerchantOffer` class and `GenerateMerchantOffers` method**

```csharp
// Assets/Scripts/Managers/RewardManager.cs — add near the top, alongside ChestReward/RunCompletionReward:

// 5.2: один предмет в ассортименте торговца — цена уже с учётом возможной скидки (Price), и
// OriginalPrice/HasDiscount для UI (зачёркнутая исходная цена и т.п.).
public class MerchantOffer
{
    public ItemData Item;
    public int OriginalPrice;
    public int Price;
    public bool HasDiscount;
}
```

```csharp
// Assets/Scripts/Managers/RewardManager.cs — add as a new public method on RewardManager:

// 5.2: множитель цены по редкости — Обычный x1.0 / Редкий x1.2 / Эпический x1.5.
static float MerchantPriceMultiplier(ItemTier tier)
{
    switch (tier)
    {
        case ItemTier.Rare: return 1.2f;
        case ItemTier.Epic: return 1.5f;
        default: return 1.0f;
    }
}

// 5.2: Цена = 100 x УровеньПредмета x Множитель(редкость).
static int MerchantPrice(ItemData item) => Mathf.RoundToInt(100 * item.itemLevel * MerchantPriceMultiplier(item.tier));

// 5.2: размер скидки, если гейт в 30% (см. GenerateMerchantOffers) сработал. Таблица вероятностей
// суммируется в 100% ВНУТРИ этих 30% (это не отдельный шанс "скидки нет" — тот уже выше).
static float RollDiscountPercent()
{
    float roll = Random.value * 100f;
    if (roll < 50f) return 10f;
    if (roll < 80f) return 25f;
    if (roll < 94f) return 50f;
    if (roll < 99f) return 75f;
    return 90f;
}

// 5.2: 5 случайных предметов, та же таблица редкости, что и сундук (см. RollItemRarity), тот же
// диапазон уровня [char; char+2] (см. RollItemLevel). 30% шанс, что ОДИН случайно выбранный из 5
// получит скидку (роллится один раз на весь визит, не по каждому предмету).
public List<MerchantOffer> GenerateMerchantOffers(int characterLevel)
{
    var offers = new List<MerchantOffer>();
    for (int i = 0; i < 5; i++)
    {
        ItemTier tier = RollItemRarity(false);
        ItemData item = null;
        if (itemCatalog != null && itemCatalog.TryGetRandomItem(tier, out var baseItem))
        {
            item = RollItemLevel(baseItem, characterLevel);
        }

        int price = item != null ? MerchantPrice(item) : 0;
        offers.Add(new MerchantOffer { Item = item, OriginalPrice = price, Price = price, HasDiscount = false });
    }

    if (Random.value < 0.30f)
    {
        var discounted = offers[Random.Range(0, offers.Count)];
        if (discounted.Item != null)
        {
            float discountPercent = RollDiscountPercent();
            discounted.HasDiscount = true;
            discounted.Price = Mathf.RoundToInt(discounted.OriginalPrice * (1f - discountPercent / 100f));
        }
    }

    return offers;
}
```

- [ ] **Step 3: Add smoke-test checks**

```csharp
// RunPureLogicChecks() — needs a RewardManager instance; if itemCatalog isn't assigned in pure-logic
// context, offers will have null Item/0 price, which is still a valid structural check:
var merchantRewardManagerGO = new GameObject("SmokeTest_MerchantRewardManager");
var merchantRewardManager = merchantRewardManagerGO.AddComponent<RewardManager>();
var offers = merchantRewardManager.GenerateMerchantOffers(3);
Check(offers.Count == 5, $"5.2 торговец предлагает 5 предметов: {offers.Count}");
int discountedCount = offers.FindAll(o => o.HasDiscount).Count;
Check(discountedCount <= 1, $"5.2 максимум 1 предмет со скидкой за визит: {discountedCount}");
UnityEngine.Object.DestroyImmediate(merchantRewardManagerGO);
```

Also add a deterministic price-formula check independent of randomness:
```csharp
var priceTestItem = ScriptableObject.CreateInstance<ItemData>();
priceTestItem.tier = ItemTier.Rare;
priceTestItem.itemLevel = 5;
// Цена = 100 * 5 * 1.2 = 600. MerchantPrice is a private static method — exercise it indirectly
// via a single-item GenerateMerchantOffers-style calculation instead of reflection: this inline
// duplication is intentional (asserting the FORMULA, not the private implementation detail).
int expectedPrice = Mathf.RoundToInt(100 * 5 * 1.2f);
Check(expectedPrice == 600, $"5.2 формула цены (Редкий, ур.5): {expectedPrice} (ожидалось 600)");
UnityEngine.Object.DestroyImmediate(priceTestItem);
```

- [ ] **Step 4: Commit**

```bash
git add Assets/Scripts/Managers/RewardManager.cs Assets/Editor/PlayModeSmokeTest.cs
git commit -m "Add RewardManager.GenerateMerchantOffers with GDD 5.2 pricing/discount formulas"
```

---

### Task 2: Merchant UI — offer cards, purchase flow

**Files:**
- Modify: `Assets/UI/GameRoot.uxml`
- Modify: `Assets/UI/GameStyles.uss`
- Modify: `Assets/Scripts/UI/RunFlowController.cs`
- Test: `Assets/Editor/PlayModeSmokeTest.cs`

**Interfaces:**
- Consumes: `RewardManager.GenerateMerchantOffers` (Task 1), `CharacterManager.RunCurrency` (existing, read-only from UI), a new `CharacterManager.TrySpendCurrency(int amount)` (spend-with-validation, mirrors `AddCurrency`), `RunFlowController.ItemCompareFlow(ItemData)` (existing, reused as-is for the "which slot" step after a purchase).

- [ ] **Step 1: Add `CharacterManager.TrySpendCurrency`**

```csharp
// Assets/Scripts/Managers/CharacterManager.cs — add near AddCurrency:

// 5.2: покупка у торговца тратит валюту забега; продавать снаряжение торговцу нельзя (только тратить).
public bool TrySpendCurrency(int amount)
{
    if (RunCurrency < amount)
    {
        return false;
    }

    RunCurrency -= amount;
    return true;
}
```

- [ ] **Step 2: Replace the merchant panel's UXML placeholder with an offers container**

```xml
<!-- Assets/UI/GameRoot.uxml — replace the current MerchantPanel block:
<ui:VisualElement name="MerchantPanel" class="panel hidden">
    <ui:Label text="Торговец" class="panel-title" />
    <ui:Label text="Торговец скоро появится." class="body-label" />
    <ui:Button name="MerchantContinueButton" text="Продолжить" class="button-primary" />
</ui:VisualElement>
with: -->
<ui:VisualElement name="MerchantPanel" class="panel hidden">
    <ui:Label text="Торговец" class="panel-title" />
    <ui:Label name="MerchantCurrencyLabel" text="Валюта забега: 0" class="body-label" />
    <ui:VisualElement name="MerchantOffersContainer" class="merchant-offers-row" />
    <ui:Button name="MerchantContinueButton" text="Уйти от торговца" class="button-primary" />
</ui:VisualElement>
```

- [ ] **Step 3: Add USS for the offer cards**

```css
/* Assets/UI/GameStyles.uss — add near .item-compare-row: */

.merchant-offers-row {
    flex-direction: row;
    flex-wrap: wrap;
    justify-content: center;
}

.merchant-offer-card {
    width: 180px;
    margin: 6px;
    padding: 10px;
    background-color: rgb(40, 40, 48);
    border-radius: 6px;
}

.merchant-offer-price {
    font-size: 14px;
    color: rgb(230, 200, 60);
    -unity-font-style: bold;
    margin-top: 6px;
}

.merchant-offer-price-original {
    font-size: 11px;
    color: rgb(150, 150, 150);
    -unity-font-style: italic;
}

.merchant-offer-discount-tag {
    font-size: 11px;
    color: rgb(90, 200, 100);
    -unity-font-style: bold;
}

.merchant-offer-buy-button {
    margin-top: 8px;
}
```

- [ ] **Step 4: Cache the new elements and rewrite `MerchantRoomFlow`**

```csharp
// Assets/Scripts/UI/RunFlowController.cs — add fields near the existing merchant fields:
Label merchantCurrencyLabel;
VisualElement merchantOffersContainer;
```

```csharp
// In CacheElements(root), replace the single-line merchant caching with:
merchantContinueButton = root.Q<Button>("MerchantContinueButton");
merchantCurrencyLabel = root.Q<Label>("MerchantCurrencyLabel");
merchantOffersContainer = root.Q<VisualElement>("MerchantOffersContainer");
```

```csharp
// Replace the entire existing MerchantRoomFlow() method:
IEnumerator MerchantRoomFlow()
{
    var offers = rewardManager.GenerateMerchantOffers(characterManager.Level);

    bool leave = false;
    while (!leave)
    {
        ShowOnly(merchantPanel);
        merchantCurrencyLabel.text = $"Валюта забега: {characterManager.RunCurrency}";
        merchantOffersContainer.Clear();

        var buttons = new List<Button>();
        foreach (var offer in offers)
        {
            var card = new VisualElement();
            card.AddToClassList("merchant-offer-card");

            if (offer.Item == null)
            {
                card.Add(new Label("Пусто") { });
                merchantOffersContainer.Add(card);
                continue;
            }

            var nameLabel = new Label(offer.Item.itemName);
            nameLabel.AddToClassList("item-card-name");
            SetRarityClass(nameLabel, offer.Item.tier);
            card.Add(nameLabel);

            var statsLabel = new Label(ItemStatsText(offer.Item));
            statsLabel.AddToClassList("body-label");
            card.Add(statsLabel);

            if (offer.HasDiscount)
            {
                var originalPriceLabel = new Label($"{offer.OriginalPrice} монет");
                originalPriceLabel.AddToClassList("merchant-offer-price-original");
                card.Add(originalPriceLabel);
                card.Add(new Label("СКИДКА!") { });
            }

            var priceLabel = new Label($"{offer.Price} монет");
            priceLabel.AddToClassList("merchant-offer-price");
            card.Add(priceLabel);

            var buyButton = new Button { text = "Купить" };
            buyButton.AddToClassList("button-primary");
            buyButton.AddToClassList("merchant-offer-buy-button");
            buyButton.SetEnabled(characterManager.RunCurrency >= offer.Price);
            card.Add(buyButton);

            merchantOffersContainer.Add(card);
            buttons.Add(buyButton);
        }

        buttons.Add(merchantContinueButton);

        yield return WaitForAnyClick(buttons.ToArray());

        if (clickedIndex == buttons.Count - 1)
        {
            leave = true; // "Уйти от торговца"
            continue;
        }

        // clickedIndex maps 1:1 into `offers` because empty-item offers still add a card but never
        // a button — so `buttons` only ever contains as many entries as offers WITH an item, plus
        // the leave button. Map back by re-walking offers with a running non-null index.
        int runningIndex = -1;
        MerchantOffer purchased = null;
        foreach (var offer in offers)
        {
            if (offer.Item == null) continue;
            runningIndex++;
            if (runningIndex == clickedIndex)
            {
                purchased = offer;
                break;
            }
        }

        if (purchased != null && characterManager.TrySpendCurrency(purchased.Price))
        {
            LogEvent($"[Торговец] Куплено: {purchased.Item.itemName} за {purchased.Price} валюты забега.");
            offers.Remove(purchased);
            yield return ItemCompareFlow(purchased.Item);
        }
    }
}
```

- [ ] **Step 5: Add a smoke-test check for the UXML rewiring**

```csharp
// RunPlayModeChecks():
RequireElement(root, "MerchantOffersContainer");
RequireElement(root, "MerchantCurrencyLabel");
```

- [ ] **Step 6: Run the full smoke test and confirm PASS**

```bash
"C:\Program Files\Unity\Hub\Editor\6000.5.8f1\Editor\Unity.exe" -batchmode -nographics -projectPath "C:\Unity Projects\DungeonGirls" -executeMethod PlayModeSmokeTest.Run -logFile -
```

- [ ] **Step 7: Manual verification in the Editor**

Enter Play Mode, start a run, force/wait for a merchant room. Confirm: 5 cards render with name/stats/price; at most one shows a discount; buying an affordable item deducts currency and opens the existing slot-choice flow; buying with insufficient currency is blocked (button disabled); "Уйти от торговца" leaves without spending a ration (check `RationsLabel` is unchanged before/after).

- [ ] **Step 8: Commit**

```bash
git add Assets/UI/GameRoot.uxml Assets/UI/GameStyles.uss Assets/Scripts/UI/RunFlowController.cs Assets/Scripts/Managers/CharacterManager.cs Assets/Editor/PlayModeSmokeTest.cs
git commit -m "Implement merchant room UI: 5 offers, pricing, discount, purchase flow (GDD 5.2)"
```

---

## Self-Review Notes

- **Spec coverage:** item 5 (merchant) fully covered by Tasks 1-2.
- **Execution-order dependency:** this plan should run AFTER Plan 1 Task 6 (rarity table 62/35/3) — `GenerateMerchantOffers` calls the shared `RollItemRarity`, so applying this plan first would (correctly, per spec) still inherit whichever table is live at the time; just note both plans touch the same method and there's no conflict, only an ordering nicety.
- **Not touched:** selling equipment (explicitly out of scope per GDD — "продать своё снаряжение торговцу нельзя").
